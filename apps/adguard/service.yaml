apiVersion: v1
kind: Service
metadata:
  name: adguard-service
  namespace: adguard
spec:
  selector:
    app: adguard
  ports:
    - name: web
      port: 3000
      targetPort: 3000
  type: ClusterIP
