apiVersion: v1
kind: Service
metadata:
  name: control-hub
  namespace: control-hub
spec:
  selector:
    app: control-hub
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
