apiVersion: v1
kind: Service
metadata:
  name: control-hub
  namespace: control-hub
  labels:
    app.kubernetes.io/name: control-hub
    app.kubernetes.io/instance: control-hub
spec:
  selector:
    app.kubernetes.io/name: control-hub
    app.kubernetes.io/instance: control-hub
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
